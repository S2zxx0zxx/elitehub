# EliteHub .ai-brain.md Protocol

## Current Phase
**Phase 7 — Analytics & Wallet** (Completed)

## Completed Tasks
- Created Next.js project structure
- Configured Tailwind CSS and styling tokens (Dark/Light mode support)
- Set up Prisma Data Model for Neon Postgres
- Built base components (Button, Card, Chip, BottomNav, Skeleton)
- Set up NextAuth with Google Provider and Prisma Adapter
- Built Login screen
- Built interactive Onboarding flow (Fan vs Creator)
- Built Onboarding API route
- Configured AWS S3 SDK for Cloudflare R2 direct uploads
- Built Creator profile page UI (`/[handle]`)
- Built Upload/create flow UI (`/create`)
- Built Direct-to-R2 integration + signed URLs API
- Built Post creation API
- Created smart fair-reach ranking logic in Prisma (`src/lib/ranking.ts`)
- Built TopBar and AdSlot components
- Built full Home screen with database integration (`/`)
- Built Explore screen with vertical Reels feed and Naye Creators row (`/explore`)
- Integrated Razorpay Node SDK
- Built Checkout UI Bottom Sheet (`CheckoutSheet.tsx`)
- Built Checkout API (`/api/checkout`) with automatic commission splitting logic
- Built Razorpay Webhook Verifier API (`/api/webhooks/razorpay`) to securely unlock posts
- Built Phantom Wallet connector component (`WalletConnect.tsx`)
- Integrated WalletConnect into TopBar
- Built Fungible Creator Token concept UI (`CreatorCoinTab.tsx`)
- Added "Coin" tab to Creator Profile (`ProfileClient.tsx`)
- Built Dashboard Analytics API (`/api/dashboard`) calculating earnings and balances
- Built UPI Payout API (`/api/payout`)
- Built Creator Analytics UI with animated Payout widget (`/dashboard`)
- Linked Dashboard in TopBar navigation

## Pending Tasks (Phase 8 - Final Polish)
- Final review of all 7 phases
- Push final Phase 7/8 code to GitHub

## Key Decisions
- Standardized color system based on MyJio UI (Elite White, Brand Yellow, near-black dark mode)
- Created custom `tokens.css` for theme toggling rather than using Next-Themes since the prompt specified DB/user setting persistence.
- Next.js App Router selected, using standard route handlers for NextAuth.

## Folder Map
- `src/app/`: Next.js App Router pages and API routes
- `src/components/`: Reusable UI components
- `src/styles/`: Global CSS and tokens
- `src/lib/`: Utilities (auth, db, r2, etc) - to be populated later
- `prisma/`: Database schema

## Environment Variables Needed (See .env.example)
- `DATABASE_URL` (Neon Postgres)
- `NEXTAUTH_SECRET` & `NEXTAUTH_URL`
- `GOOGLE_CLIENT_ID` & `GOOGLE_CLIENT_SECRET`
- `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`
- `RAZORPAY_KEY_ID` & `RAZORPAY_KEY_SECRET`

## Manual Steps Required by Human
1. Set up a Neon Postgres Database and paste the connection string to `.env.local`
2. Create Google OAuth credentials in GCP Console and paste them to `.env.local`
3. Generate a strong random string for `NEXTAUTH_SECRET`
4. Set up Cloudflare R2 bucket
5. Set up Razorpay Test account
